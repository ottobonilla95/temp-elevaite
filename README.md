# temp-elevaite

A minimal, scrollable Next.js app with a title and four text sections.

## Development

```sh
npm install
npm run dev
```

## Production

```sh
npm run build
```

The static site is generated in `out/`.
